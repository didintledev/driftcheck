# Copy to .env and fill in. .env is gitignored — never commit a real key.

# Default provider. Free tier, no card: https://aistudio.google.com/apikey
GEMINI_API_KEY=...

# Only needed for --provider anthropic (requires prepaid API credits).
# ANTHROPIC_API_KEY=sk-ant-...
